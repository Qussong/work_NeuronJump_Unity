using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace GH
{
    public delegate void DvoidDelegate();
    public delegate void DPacketDelegate(byte[] bytes);
    public delegate bool DreturnBoolDelegate();
}
